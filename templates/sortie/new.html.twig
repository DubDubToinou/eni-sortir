{% extends 'base.html.twig' %}

{% block title %}Organiser{% endblock %}

{% block body %}
    <h1>Organiser une sortie</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}

    <button class="btn" name="save">sauvegarder</button>
    {{ form_end(form) }}

    <a href="{{ path('app_main') }}">Annuler</a>
{% endblock %}
