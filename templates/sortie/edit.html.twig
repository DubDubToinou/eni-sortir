{% extends 'base.html.twig' %}

{% block title %}Edit Sortie{% endblock %}

{% block body %}
    <h1>Edit Sortie</h1>

    {{ include('sortie/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_sortie_index') }}">back to list</a>

    {{ include('sortie/_delete_form.html.twig') }}

    {% if sortie.organisateur is same as (app.user) %}
    <a href="{{ path('app_sortie_annulation', {'id':sortie.id}) }}">Annulation</a>
    {% endif %}
{% endblock %}
